# Model catalog snapshots

See [Model discovery and fallback snapshots](../docs/MODEL_CATALOG.md) for source precedence, supported adapters, the export/update workflow and validation commands.

Edit the JSON data here rather than adding model lists to Rust. Initial snapshots are curated; live exports identify their observation time and completeness.
